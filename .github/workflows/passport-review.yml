name: Software Signal Passport
on:
  pull_request_target:
    types: [opened, synchronize, reopened, edited, ready_for_review]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      pr:
        description: Pull request number to reassess
        required: true
permissions: {}
concurrency:
  group: passport-${{ github.event.pull_request.number || github.event.issue.number || inputs.pr }}
  cancel-in-progress: false
jobs:
  passport:
    if: >-
      github.event_name != 'issue_comment' ||
      (github.event.issue.pull_request && startsWith(github.event.comment.body, '/passport '))
    permissions:
      contents: read
      pull-requests: write
      issues: read
      checks: read
      actions: read
    uses: suyog19/software-signal-passport/.github/workflows/passport.yml@0489c941d1107ab179f5c212cd1b69754997d804
    with:
      runtime-ref: 0489c941d1107ab179f5c212cd1b69754997d804
      pr: ${{ github.event.pull_request.number || github.event.issue.number || inputs.pr }}
    secrets:
      model-key: ${{ secrets.PASSPORT_MODEL_KEY }}
